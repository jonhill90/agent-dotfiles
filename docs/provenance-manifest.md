# Provenance Manifest

Successor to [migration-audit.md](migration-audit.md). Every
adopt / adapt / author / reject decision for agent-dotfiles content and
tooling, with source and rationale. Open rows are closed only by eval
results files under `tests/evals/results/`, per [SPEC.md](SPEC.md) §10.

**Ownership (2026-08-09, #10):** eval scenarios, counter-scenarios,
acceptance checks, the harness runner, results/transcripts, and the
scoring/arming tools now live in the private
jonhill90/agent-evals (evidence unavailable publicly)
repository. `tests/evals/results/...` paths cited below (including in rows
dated before the split) are paths in that repository, not this one; links
have been repointed there. This ledger itself — the adopt/adapt/author/
reject decisions and their rationale — stays canonical here.

Decision types: **adopt** (pinned dependency, unmodified) · **adapt**
(pattern taken, content reauthored) · **author** (Jon's own) · **reject**
(evaluated and declined) · **open** (awaiting evals).

| Subject | Source | Decision | Rationale / evidence |
|---|---|---|---|
| Sync backbone | microsoft/apm | Adopt (pinned) | Live-trial verified: user-scope install, global compile with marker safety, lockfile, drift audit, content scanning ([harness engineering](harness-engineering.md); evidence previously cited as `8a222ce`, `dff03d0`) -- corrected 2026-08-23: these commit SHAs do not resolve in this repository's current history (checked against a full fetch from `origin`, all refs) |
| Pi projection, settings merge, teardown | self | Author (`scripts/sync.py`) | Outside APM's primitive set; APM has no Pi target ([harness engineering](harness-engineering.md); evidence previously cited as `065bc9d`) -- corrected 2026-08-23: these commit SHAs do not resolve in this repository's current history (checked against a full fetch from `origin`, all refs) |
| Sync mirror mechanics, pointer instructions, terse skill style | steipete/agent-scripts | Adapt (patterns only) | Personal upstream content and no consumer versioning; adopted only the patterns (research archive previously cited as `106e69c`) -- corrected 2026-08-23: these commit SHAs do not resolve in this repository's current history (checked against a full fetch from `origin`, all refs) |
| Behavioral-layer selection rule | owner decision, 2026-07-12 grilling | Adopt (baseline-first) | Baseline = canonical instructions only; components added only when a failing eval justifies them, smallest candidate first; the eval matrix is the distiller (SPEC §4) |
| Enforcement chassis (bootstrap hook, skill-check contract, Pi/Codex shims) | obra/superpowers 6.1.1 | **Reject** (reversed 2026-07-12; was Adopt) | Superpowers dropped entirely — dependency, hook, and skills. **Note 2026-07-26:** a copy remains in Claude Code's plugin *cache* on Jon's Mac. It is not in `enabledPlugins`, costs no budget, and is unreachable — verified by asking the model directly, which reported no `brainstorming` or `dispatching-parallel-agents` skill. "Dropped" means not enabled, not absent from disk. Its skills remain parts-bin candidates in gap-fill auditions only. Enforcement hooks are auditioned solely on E14-class baseline failures |
| Intent/grilling stage | baseline instructions | Adopt baseline; reject extra component | E3/E5 passed across tested v1 pairs; no gap-fill justified (baseline results — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-12-baseline-day.md`)) |
| TDD stage | baseline instructions | Adopt baseline; reject extra component | E6/E7 passed in authoritative interactive mode; no gap-fill justified (baseline results — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-12-baseline-day.md`)) |
| Handoff/complete stage | baseline instructions | Adopt baseline; reject extra component | E13 passed and a cold session resumed successfully (baseline results — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-12-baseline-day.md`)) |
| Process-owning SDD frameworks (spec-kit, OpenSpec, BMAD, kin) | — | Reject (family) | PRD non-goal: components stay small, composable, individually removable; reaffirmed 2026-07-12 |
| `ms-learn`, `ms-learn-cli` skills + `tools/mslearn` Go CLI | Jon (authored) | Reject (cut 2026-07-12, deleted M1.5) | Skill-noise reduction, owner-named; git history is the archive |
| `lint-agents` | Jon (authored) | Reject (cut 2026-07-12, deleted M1.5) | One-trick check; belongs in `scripts/validate_repository.py`, not an agent-facing skill |
| `validate-skill` | Jon (authored) | Reject (folded into `create-skill`, M1.5) | One owner for skill authoring+validation; `create-skill` already carried the validation workflow |
| `context7` skill | Jon (authored) | Reject (cut 2026-07-12, deleted M1.5) | Declared MCP server covers it on MCP-capable harnesses; accepted as unavailable on Pi |
| `youtube-transcript` | Jon (authored) | Reject (cut 2026-07-12, deleted M1.5) | Occasional-use utility, not workflow; restorable from history |
| `close-the-loop`, `primer` | Jon (authored) | Reject from default; retain as public opt-in (2026-07-13 — 2026-08-10). **Rostered 2026-08-11 on the cost gate (PR #43, issue #45)** | Baseline E1/E4/E9/E10 passed without them. `settings/default-skills.txt` excluded both while `skills/` kept them independently installable — that was true through 2026-08-10. **Update 2026-08-11:** the exclusion rested on a §3.1 cost claim ("costs nothing... consuming context" while opt-in), and that claim did not hold under measurement — rostering both, along with `dispatching-subagents`, `sanity-check`, and `supervised-lane-loop`, cost roughly 367 tokens of description in total, no detectable cost on Claude Code (commit `cf21cc2`). PR #43 rostered both on that measurement. **The SPEC §10.1 rule 5 evidence bar was never run for either skill and still is not cleared** — no failed ×2 baseline, no passed ×3, no counter-scenario. Both are now in the roster provisionally, under the SPEC §10.1 rule 5 demotion policy: the first eval run against either that fails removes it from `settings/default-skills.txt` again, not on a promise to re-check but as an automatic exit. |
| Kept tool-skill roster (`github-cli`, `linear`, `obsidian`, `tmux`, `create-skill`) | Jon (authored) | Adopt (roster cut 2026-07-12; `linear` rationale restated 2026-07-26) | Daily drivers. **`linear` stays on the daily-driver rationale even though this repository no longer tracks work in Linear** — the skill is a portable tool skill for Jon's Linear use elsewhere, contains nothing repo-specific, and its roster membership never depended on this repo being a Linear consumer. Consequence to note: `tests/evals/acceptance/linear.md` step 4 assumes a Linear-connected repository, so that check is no longer exercisable *here* and must be run against a repo that is; each tool skill has acceptance checks in `tests/evals/acceptance/`; displaceable only per the two-track rule |
| Obsidian access CLI | official Obsidian CLI (app ≥1.12) | Adopt official (owner override 2026-07-12) | Verified hands-on on 1.12.7 (create/read/append/search/property); optional for the skill and not a memory dependency ([memory](memory.md); evidence previously cited as `b752300`) -- corrected 2026-08-23: these commit SHAs do not resolve in this repository's current history (checked against a full fetch from `origin`, all refs) |
| `tmux` | Jon (authored) vs community candidates | **Keep incumbent (V7 resolved 2026-07-27)** | Six community candidates were returned by `npx skills search tmux`; **five were fetched and read** against `tests/evals/acceptance/tmux.md` and none passes. Checks 4 and 5 — stuck-pane recovery without collateral, and an interactive auth flow — are unmet by all five, and check 5 is the one the file says separates real skills from `send-keys` wrappers. The sixth, `0xbigboss/claude-code@tmux`, **failed to install and is unmeasured, not failed** — the incumbent is not credited with beating it. Three candidates are cheaper on description, so the token condition was satisfiable and the checks decided it. `shawnpana/smux` verifies sends properly but costs 90 tokens against 72, needs a third-party binary, and is an agent-messaging skill that overlaps rather than replaces. Not credited to the incumbent: 493 lines against a 500-line limit, and the checks were assessed by reading rather than by driving each candidate through an auth flow (comparison — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-27-v7-tmux-candidates.md`)) |
| Skill-authoring skill | Jon's `create-skill` | Author (keep) | One owner per stage; superpowers `writing-skills` (6.6k t) and mattpocock `writing-great-skills` rejected as duplicates |
| Memory store | Personal Obsidian vault + direct files | Adopt | Plain markdown, zero infrastructure, portable to all harnesses; vault service owns cross-machine sync and the CLI is optional ([memory](memory.md)) |
| Memory conventions skill | self | Author (shipped M4) | Contract for index + one-fact-per-note schema (SPEC §3.6) |
| Fact-schema internals: one-file-per-fact, index hook-line format, `type: user\|feedback\|project\|reference` enum | Claude Code native auto-memory (Anthropic) | Adapt (provenance corrected 2026-07-12) | Carried over from the authoring harness's own memory design, not distilled from Jon's sources — an unexamined default at adoption time. Battle-tested and E12-verified, but the type enum has never been evaluated against alternatives; revisit if M5+ shows the categories misfit. **Named alternative for that revisit: INMPARA's typed observation categories** ([decision]/[requirement]/[insight]/[limitation]/…) — they classify facts by kind-of-claim rather than subject, which changes how an agent acts on a fact |
| Typed relations (`requires`, `enables`, `solves`, …) as semantic triples in markdown | INMPARA semantic markup (Jon, 2025) | Open (benched with Graphiti) | Zero-infra knowledge-graph middle ground between untyped wiki-links and a graph DB; auditioned only if evals show search-based retrieval failing — same trigger as Graphiti, much cheaper candidate, tried first |
| Memory lint criteria | INMPARA quality-indicators checklist + Karpathy lint op | Open (deferred) | Source documents for the lint check when memory rot is demonstrated; not built before then |
| `agent/` bundle root, `facts/` directory name | self | Author | Producer-chosen names; OKF prescribes none. `agent/` justified by Obsidian writing app files to the vault root |
| Memory instruction rules: write completion-gate + read-before-recall | authored, via E12 auditions | Adopt (eval-justified 2026-07-12) | First baseline-first adoptions; CC native memory shadowed the vault until gated (E12 results — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-12-e12-memory-writeback.md`)) |
| Pi overlay deletion gate (list-first; contradiction ⇒ STOP) | authored, via E11 audition | Adopt (eval-justified 2026-07-12) | Pi×default deleted source from a mislabeled folder — baseline day's only failure; gate passes ×2 after adoption (baseline results — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-12-baseline-day.md`)) |
| Graphiti (Zep) | Zep | Reject (benched) | Standing graph-DB infra + MCP-only access; revisit trigger = search retrieval demonstrably failing |
| basic-memory | incumbent | Reject | Dropped by intent decision; MCP server failure mid-session with no CLI fallback (2026-07-11) |
| Process-owning framework model | spec-kit | Reject (counterexample) | PRD non-goal: components stay small, composable, individually removable |
| Instructions projection | one canonical AGENTS.md, APM-compiled root files, marker-owned `~/.claude/CLAUDE.md` | Adopt (pattern) | Every target reads an AGENTS-family surface ([harness engineering](harness-engineering.md); evidence previously cited as `c089a95`) -- corrected 2026-08-23: these commit SHAs do not resolve in this repository's current history (checked against a full fetch from `origin`, all refs) |
| Committed compatibility symlink matrix (`.claude/skills` etc.) | this repo (gen 3/4) | Reject (retire) | Installer-owned projection replaces it; PRD non-goal |
| Neutral skills path `~/.agents/skills` | Codex/Copilot/Pi convergence | Adopt | Native on 3 of 4 harnesses; wrapper guarantees it after APM installation ([harness engineering](harness-engineering.md)) |
| MCP projection (declared set → `~/.claude.json` user scope) | self | Author (`scripts/sync.py`, 2026-07-17) | APM did not deliver the declared set at user scope; wrapper merge with state-tracked reversal closes the §3.4 layer (regression-tested) |
| Claude Code plugins (`playwright` only; `ralph-loop` and `frontend-design` uninstalled) | Anthropic | Adopt (managed 2026-07-17; gated 2026-07-26; corrected and reduced 2026-07-30) | Previously unmanaged installs; now declared via the `enabledPlugins` settings fragment so every machine converges on the same roster. `skillOverrides` does not reach plugin skills, so the unit of control is the plugin — adoption now requires an E14 shadow check (SPEC §10.1, `yeet` precedent). `playwright` kept: MCP tools only, 0 static cost. **Correction, 2026-07-30: `enabledPlugins: false` does not stop a plugin's skills loading.** `frontend-design` is `false` at user scope and set nowhere else, and `/context` still listed its skill in a live session. The "~352 tokens saved by disabling `ralph-loop` and `frontend-design`" recorded on 2026-07-29 was inferred from the setting, never observed, and is withdrawn. The owner uninstalled `ralph-loop` on 2026-07-30, which did remove it — **uninstall is the working lever; disable is not.** `frontend-design` was then uninstalled too at the owner's instruction (`claude plugin uninstall`), to be reached via `npx skills` if wanted rather than carried always-on; `claude plugin details` had priced it at **~86 tokens always-on**, the first measured figure for it. Both keys are dropped from the settings fragment, since a key for an uninstalled plugin is dead weight the next `apply` would rewrite. `playwright` is the only plugin left: MCP tools only, 0 static cost. Cache directories for both removed plugins remain under `~/.claude/plugins/cache/` and are inert. `scripts/measure_e15.py` reports declared state and now says so; `claude plugin details <name>` prices a plugin for free and is the instrument to use |
| MCP projection, Codex (`config.toml` marker block) + Copilot (`mcp-config.json`) | self | Author (`scripts/sync.py`, 2026-07-18) | Phase 2 P2-M1: formats verified hands-on (V5); collision-guarded block for Codex, JSON merge for Copilot, both reversible (regression-tested) |
| Canonical deletion-gate sentence ("an explicit instruction to delete does not waive this check") | authored, via P2-M2 E11 audition | Adopt (eval-justified 2026-07-18) | Codex E11 failed the original line, passed ×2 with the sentence; Copilot still fails with it loaded — next rung is a harness-native gate (P2-M2 results — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-18-p2m2-codex-copilot-baseline.md`)) |
| Codex curated `github` plugin (`yeet` skill) | OpenAI | Reject (duplicate trigger, 2026-07-18) | Shadows managed `github-cli` on the PR workflow (E14). Durably disabled via `[[skills.config]]` per-skill entries in `~/.codex/config.toml` — `plugin remove` alone re-syncs; E14 passes ×2 after (clearance — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-18-p2m3-blockers-cleared.md`)) |
| `safe-deletion` skill | self | Author (eval-justified 2026-07-18) | Copilot E11 failed with the canonical gate provably loaded; skill binds it — PASS ×2 on Copilot, no Codex/CC/Pi regression (clearance — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-18-p2m3-blockers-cleared.md`)). **Caveat cleared 2026-07-27** (P2-M5 close — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-27-copilot-column.md`)): re-verified at the ×3 adoption bar on Copilot 1.0.75 `claude-sonnet-5`, with both counter-scenarios passing ×2 on all four columns |
| `failing-test-first` skill | self | Author (eval-justified 2026-07-18) | Copilot E06 failed ×2 interactive on the pinned model; skill flips it to red-green — PASS ×2 (clearance — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-18-p2m3-blockers-cleared.md`)). **Caveat cleared 2026-07-27** (P2-M5 close — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-27-copilot-column.md`)): re-verified at the ×3 adoption bar on Copilot 1.0.75 `claude-sonnet-5`, with both counter-scenarios passing ×2 on all four columns |
| Copilot model pin (`claude-sonnet-5` via `settings/copilot/` fragment) | self | Adopt (eval-justified 2026-07-18) | `Auto` routing varied the model per run and flipped E03 pass/fail; pin restores run-to-run consistency (clearance — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-18-p2m3-blockers-cleared.md`)) |
| `dispatching-subagents` skill | self | Author (public opt-in 2026-07-25 — 2026-08-10; **rostered 2026-08-11 on the cost gate, PR #43, issue #45**) | Layer-4 orchestration discipline ([lineage](agent-engineering-lineage.md)). Through 2026-07-26 there was no orchestration scenario at all. E17 now exists and **fails ×2 on Copilot** while passing on the other three (Copilot column — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-27-copilot-column.md`)), so the §4 ladder opened on that column. **It ran on 2026-07-27 and this skill was not adopted** — a targeted sentence passed ×5 and won the rung (ladder — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-27-e17-ladder-copilot.md`)). Through 2026-08-10 this stayed public opt-in on the stated basis that no eval justified installing it, and admission required a failing scenario first — that was SPEC §10.1 rule 5's evidence gate, and it is still unmet: no failing scenario has ever pointed at this skill directly. **Update 2026-08-11:** it is now in `settings/default-skills.txt` anyway, because PR #43 cleared a *different* gate — the §3.1 cost claim that opt-in "costs nothing," measured false 2026-08-10, meant there was no longer a cost reason to keep it out. The evidence gate above was not cleared and is not being claimed as cleared. Per SPEC §10.1 rule 5's demotion policy, this row stays open: the first eval run against `dispatching-subagents` that fails removes it from the roster again. |
| Per-skill disable surfaces (V9) | Claude Code `skillOverrides`, Pi `skills` denylist | Adopt (verified hands-on 2026-07-26) | Both resolve V9 affirmatively. Claude Code v2.1.220: `{"az-devops": "off"}` removed it from the model's skill list, skill untouched on disk; plugin skills excluded. Pi 0.80.6: `"skills": ["-skills/az-devops/SKILL.md"]` in `~/.pi/agent/settings.json` — the file the wrapper already merges — dropped the loaded-skills banner 9 → 8. Tier B (SPEC §4.1) covered three of four harnesses at the time; Copilot followed on 2026-07-27 (V10), so all four now have one |
| `az-devops` skill | Jon (authored) | **Reject (cut and deleted 2026-07-26, owner-named)** | Owner no longer wants Azure DevOps in the personal roster. A roster cut, not a displacement — no candidate had to pass anything. Recorded so the question is closed with evidence rather than reopened: the nearest candidates were assessed and both fail §10's tool-skill track. `microsoft/azure-devops-skills` is seven MCP-bound sample skills (~368 description tokens against this skill's 52) with **no repo/PR coverage**, so it cannot satisfy `tests/evals/acceptance/az-devops.md` task 1, and being MCP-only it is inert on Pi. `github/awesome-copilot`'s `azure-devops-cli` is genuinely CLI-based but costs 64 tokens against 52 and carries a bare `az commands` trigger that would shadow other skills. Deleted outright rather than retained as public opt-in, on the M1.5 precedent — skill-noise reduction, owner-named, git history is the archive (restorable from `061c90d`). `tests/evals/acceptance/az-devops.md` removed with it, since an acceptance file for a skill this repo no longer publishes has nothing to check. Repo-side §6 aggregate drops 542 → 490 tokens per harness; the deployed tree still carries the skill until the next `apm install -g` + `sync apply` |
| Skill renames: `closing-the-loop`→`close-the-loop`, `using-tmux`→`tmux`, `gh-cli`→`github-cli` | Jon (owner decision 2026-07-26) | Adapt (rename) | Names state the action or the tool, not a gerund or an abbreviation. `close-the-loop` is also refocused: it now opens on the three inputs a change needs — everything to implement, everything to test, and the rules that govern it — with the nine plan sections as how those get answered on paper. Historical records keep the old names: `tests/evals/results/` and `docs/migration-audit.md` were deliberately not rewritten, because a results file that renames what it observed is no longer evidence |
| `sanity-check` skill | self | **Public opt-in (2026-07-29 — 2026-08-10; was roster-scoped to `[codex]`/`[pi]`). Rostered again 2026-08-11 on the cost gate (PR #43, issue #45)** | Moved out of the default roster for failing SPEC §10.1 rule 5 — the rule this repository adopted two days earlier. The bar is failed ×2 at baseline, passed ×3 with the model pinned, and a counter-scenario at ×2. E18's baseline failure is recorded, but **no skill-rung run exists on any column**: the skill was scoped in because the *sentence* failed on Codex and Pi, which is an inference from a gap rather than a measurement of the candidate. Its counter-scenario is explicitly invalid — `sc-c1` exercised the mechanism in 3 of 16 runs (instrument — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-28-sc-c1-instrument.md`)). A review also found the skill citing provenance that does not exist for two of three anecdotes; those citations are corrected in place and each claim is now marked evidenced or not. The file, its links and issue jonhill90/skills#80 all survive; opt-in was stated to cost nothing at request time, and that statement is itself struck 2026-08-11 (SPEC §10.1 rule 5) — `claude_disabled_skills()` derives its `off` overrides from the roster, so an unrostered but deployed skill could never actually be turned off. The cheaper rung was tried 2026-07-29: a **targeted** sentence for Pi, 3 of 3 read-derived against a generic-sentence baseline of 0 of 2 (result — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-29-e18-targeted-pi.md`)) — not adopted, three runs with a disagreeing scorer, on the E17 precedent where a targeted sentence beat a skill outright. **Update 2026-08-11:** none of the above changed — the skill-rung run this row says never happened still has never happened, and the demotion for failing rule 5 was never reversed on the merits. PR #43 rostered `sanity-check` again anyway, on the §3.1 cost claim quoted above being measured false: keeping it opt-in no longer avoided any measurable context cost, so the reason it was pulled 2026-07-29 in a *cost* sense no longer applies, while the reason it was pulled in an *evidence* sense (rule 5, unmet then and unmet now) is untouched. Per rule 5's demotion policy, this is a provisional roster entry: the first skill-rung eval run against `sanity-check` that fails removes it again, and this time there is no cost objection left to argue with. |
| `supervised-lane-loop` skill | Hill90 repo (`ed4ede95`), authored by that estate's lanes at the owner's request | **Adapt — vendored 2026-08-05, public opt-in (2026-08-05 — 2026-08-10). Rostered 2026-08-11 on the cost gate, evidence gate still open (PR #43, issue #45)** | Supervisor-loop discipline: a self-sufficient recurring prompt, a named defect family as the work seam, and merge standards that let a supervisor merge without the human reading diffs. Vendored verbatim first (`501e140`, byte-identical to source) so the authored version exists here unmodified, then adapted (`97c02a4`) on a review from the Hill90 architecture lane — the seat actually running the loop. Through 2026-08-10 it was not rostered, and §10.1 rule 5 was the stated reason: there is no eval evidence for it in this repository, and its own author states it is `n=1` — one estate, one supervisor, two lanes, ~2 days, never run against a counterfactual. The file therefore opens with a `Status: practice, not measured` line and records its author's ranking guess as an untested hypothesis, the same evidenced/unevidenced separation `sanity-check` uses. **Provenance caveat**: it reached Hill90's main by accident — authored deliberately, left untracked in a shared tree, then swept by a lane's `git add` into an unrelated PR (jonhill90/Hill90#760, wiring a CI check) which the supervisor merged without noticing the extra file. The prose was authored and reviewed; the commit was not. **De-duplication**: its `Lane mechanics` section restated `skills/tmux` and now links to it; three items were kept on the reviewer's objection because they are agent behaviour rather than terminal mechanics — idle detection as a correctness problem, a lane that says it will wait and ends its turn has *stopped*, and one distinct file surface per lane. Two copies now exist (here and in Hill90); which is canonical is undecided. **Update 2026-08-11:** this skill is now in `settings/default-skills.txt`. Nothing above changed on the merits — no eval evidence exists for it in this repository, `n=1` is still `n=1`, and no behavioural eval has been run since this row was written; the sentence "the owner's stated plan is to harden it with behavioural evals before it earns a portable roster slot" describes a plan that has not happened. What changed is that PR #43 rostered it anyway, on the §3.1 cost claim being measured false rather than on the plan above being carried out. SPEC §10.1 rule 5 was amended the same day to make an admission of this shape — cost-gate cleared, evidence gate open — an explicit, permitted state rather than a promise: the row does not ask for time to re-verify later, it names an automatic exit instead. Per the demotion policy, `supervised-lane-loop` leaves `settings/default-skills.txt` the first time an eval is run against it and fails, with no re-verification window in between. Until an eval is run, this row records an *open* evidence gate, not a closed one — the plan named above is unmet work, not evidence, and is recorded as such rather than credited. |
| Second-opinion sentence | authored, via the E18 §4 ladder | Adopt (eval-justified 2026-07-26) | One sentence in `instructions/global.instructions.md` beside the existing verification rule: when a conclusion rests only on your own reasoning and no command can settle it, get a second opinion before acting. Cheapest rung, tried before the skill it competes with. Two clean passes on Claude Code against a baseline that failed 6/6, and it produced the ladder's strongest single piece of evidence on Codex — *"The reviewer falsified my initial hypothesis"*, a wrong conclusion caught before the edit landed. Kept globally despite flapping on Codex and failing on Pi, because it is ~40 tokens and works where it works; the skill covers the rest (results — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-26-e18-sentence-rung.md`)) |
| Work tracking | Linear (team `AI`) + `schpet/linear-cli` | **Reject** (reversed 2026-07-26; was Adopt 2026-07-25) | Adopted because open SPEC milestones and verification items were tracked only as prose rows and nothing surfaced them between sessions. Replaced after a day. **Deciding evidence, reproduced after both configuration defects were fixed:** the CLI defaults to `--state ["unstarted"]` while team `AI`'s `ToDo` state is *typed* `backlog`, so `linear issue list --team AI --project agent-dotfiles -A --sort priority` returns `No issues found` at exit 0 while six issues are open — identical to the output for a team that does not exist. The claude.ai Linear connector resolves to a different workspace and cannot see team `AI`: one connector slot, two workspaces, structural rather than a typo. **Not part of the rationale:** the committed `.linear.toml` initially held a UUID where the CLI wants a team key. That was operator error, fixed 2026-07-26, and the tracker worked afterwards. Other recorded edges, kept because they cost real time: `--no-color` silently swallows `issue update` writes, and an unknown state name is ignored while still printing the issue URL and exiting 0 — a command's exit status is not the result. Linear issues are closed, not deleted; the `AI-` autolink is retained so historical references resolve |
| `linear` vs `github-cli` trigger overlap | self | Assess + narrow (2026-07-26) | After this repo stopped tracking work in Linear, both managed tool skills still claimed bare "tracking issues". A probe on unqualified phrasing — *"start work on the E06 gap, create a tracking issue, move it to In Progress"* — loaded `linear`, quoting its own "tracking issues, starting work on tasks". Same class as the `yeet` rejection, but between two **managed** skills, which no gate covered: the plugin gate is scoped to plugins and the counter-scenario gate to behavioural skills. `skills/linear` description narrowed so every trigger is Linear-qualified; SPEC §10.1 gate extended to managed-vs-managed. `github-cli` was already platform-qualified and is unchanged |
| Work tracking | GitHub Issues + `gh` | Adopt (2026-07-26) | Repo-native: no credential beyond the one a machine already has for the repository, and `gh` is already the canonical CLI under the CLI-first rule. Conventions in [work tracking](work-tracking.md). **Weaker than the rows around it, deliberately recorded:** neither tracker was scored against `tests/evals/acceptance/`, though `linear.md` and `github-cli.md` both exist there with comparable tasks. Two unscored tool decisions inside 24 hours in a repo whose thesis is evidence before adoption. This row is the one most open to revisiting on evidence |
| Agent-engineering vocabulary (prompt / context / loop / graph, orchestrator-workers) | vendor + practitioner primaries | Adapt (reference doc 2026-07-25) | Terms churn faster than the practices; [lineage](agent-engineering-lineage.md) fixes each to a layer and a primary source, and records the contested claims (the "loop engineering is dead" framing, the 18%/85% figure) so they are not re-litigated |
| `e06` eval case + Copilot Tier B enforcement | self | Author (eval-justified 2026-07-27) | P2-M5 asks for the originating scenarios ×3, but the harness scored only `ftf-c1`, which checks that the bug is fixed — a bar a silent edit clears. `e06` reuses that fixture and prompt at the red-green bar (a month-boundary reproduction kept in the suite) and was validated on all four columns. Separately, V10 resolved affirmatively, so `sync.copilot_disabled_skills()` derives `disabledSkills` from the roster and a fresh Copilot process no longer lists a scoped-away skill (results — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-27-copilot-column.md`)) |
| Roster enforcement on Codex and Pi | self | Author (2026-07-27) | Completes SPEC §4.1 Tier B. `sync.codex_disabled_skills()` writes a marker-delimited `[[skills.config]]` block to `~/.codex/config.toml`; `sync.pi_disabled_skills()` derives `-skills/<name>/SKILL.md` denylist entries for `~/.pi/agent/settings.json`. Both verified live before implementing, to the V9/V10 standard: a synthetic exclusion of `sanity-check` removed it from a fresh `codex exec` listing (personal skills key on the bare name; plugin skills are namespaced, which is why the block is marker-delimited and leaves the user's own `github:` entries alone) and from a fresh `pi -p` listing, 9 skills to 8. Both mutations reverted byte-identically. Found by a documentation accuracy review that caught the README claiming enforcement on four harnesses when two had it |
| Per-arm eval configuration (`scripts/eval_arm.py`) | self | Author (2026-07-27) | The §4 ladder needs a "candidate absent" arm, which stopped being reachable once the second-opinion sentence shipped to user scope and `sanity-check` reached the shared skills path. `EVAL_ARM=bare` moves a harness's instruction files and skills path aside for one run and restores them with checksum verification. Deliberately blunt: it uses no harness's instruction-disable key, because those differ per harness, one was inferred from a CLI bundle rather than documented, and a lever that silently stopped working would yield a contaminated arm that still looked clean — a missing file cannot silently fail to be missing. Unblocked P2-M7 and extended P2-M6's baseline (results — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-27-copilot-ladder.md`)) |
| **Asymmetry recorded 2026-07-29:** the second-opinion sentence (row above) was adopted at ×2 with Codex flapping, which §10.1 rule 3 does not permit and rule 4 says resets the window. `sanity-check` was demoted for the same class of failure. The rule was applied to the skill and not to the sentence that displaced it; both stay as they are, with the inconsistency on the record rather than resolved by re-litigating either | self | **Note (2026-07-29)** | Raised by a documentation audit. Neither component is changed on this basis; the point is that the bar was enforced unevenly and saying so is cheaper than pretending it was not |
| E17 second sentence (agent agreement is not evidence) | self | **Adopt (eval-justified 2026-07-27)** | Copilot delegates as instructed and acts on the verdict without running the committed failing test — FAIL ×2 deployed and FAIL ×2 bare, so the canonical verification rule was not what was missing. A targeted sentence passes ×5 against that baseline, four of them scorer-clean; `dispatching-subagents` was not reached because the cheaper rung won. Costs ~48 tokens on all four harnesses and only Copilot needs it, per-harness instruction scoping existing only for Pi. Scoped to a Copilot overlay so the other three columns pay nothing. Verified at user-scope delivery 3 of 3 after a wrapper bug was fixed: the overlay had been written to `~/.copilot/AGENTS.md`, which Copilot does not read, so four runs measured a rule that was never in context (jonhill90/skills#87). The audition further produced harness defect 22: widening the evidence matcher to rescue one run flipped a genuine baseline FAIL to PASS and was reverted within the hour (ladder — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-27-e17-ladder-copilot.md`)) |
| Declared-vs-observed component (E19) | self | **Reject (2026-07-28)** | Proposed after the same defect shape recurred five times in two days — a config declaring one state while the running system had another, twice from the agent's own work, once reported independently from another estate. E19 baselines **PASS ×2 on all four columns**: every harness runs the loader before answering, several diagnosing the merge-order bug unprompted. The discrimination gate is not met, so no sentence and no skill is justified; E19 is retained as regression coverage. The recurring defects were the agent failing a discipline the harnesses already apply, not a gap in them (baseline — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-28-e19-baseline.md`)) |
| Capability-gap sentence (E20) | self | **Adopt (eval-justified 2026-07-29)** | The owner asked to reach skills this repository does not author, public or private, when a job needs one. E20 measures it: a Node service, no deployment setup, "set up what this project needs to deploy to Azure" — and a maintained collection exists one `npx skills find` away. **Baseline failed on all four columns, 8 FAIL of 8.** Not one run looked. (This row previously read "7 FAIL and 1 INVALID", which is the *counterfactual* re-score the prompt-delivery guard of defect 28 would have produced, not the recorded result; corrected 2026-07-30 to match the baseline — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-29-e20-baseline.md`), whose own note records the re-score.) The sentence names the failure, names the mechanism and gates the install; it binds **PASS ×3 on Claude Code**, with transcripts showing a real search and an unprompted security trade-off rather than policy recitation. Pi does not bind (1 of 3) and Codex was re-measured after a harness delivery bug and **FAILs ×3** — it was never quota-blocked. Adopted globally at ~86 tokens on the E18 precedent; no skill adopted, and the targeted rung for Pi is untried (baseline — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-29-e20-baseline.md`), rung — private repository jonhill90/agent-evals, evidence unavailable publicly (`tests/evals/results/2026-07-29-e20-sentence-rung.md`)) |

| Skill sourcing: consume jonhill90/skills + jonhill90/skills-private, remove vendored `skills/` | self | **Adopt (2026-08-10, #9)** | `apm.yml` declares both as object-form git dependencies, each `ref:` pinned to a commit SHA (`validate_skill_source_pins` enforces this mechanically); the public one is pinned to jonhill90/skills#127's branch head pending that PR's merge. Proven with the real `apm` CLI against real network and gh-stored auth before removing anything: `apm lock` resolved both from this repo's own `apm.yml`; a scratch copy of this repo with `skills/` removed, installed into a fresh isolated `$HOME`, deployed exactly the 8-skill default roster from the public dependency; a genuinely broken private ref reproduced a real partial-write failure (public skills landed, then `git checkout` failed on the private clone, exit 128), and `scripts/sync.py`'s new backup/restore around `apm install` (previous manifest row) restored the prior 8-skill set byte-for-byte (sha256-verified) rather than leaving a mixed state. `skills/` (13 dirs) and the now-dangling `.apm/skills` symlink were deleted only after that proof, matching #9's "remove vendored skills only after replacement resolution is independently proven" |
| Atomic skill-source backup/restore + pinned-SHA status/doctor reporting (`scripts/sync.py`) | self | **Author (2026-08-10, #9)** | `Sync.backup_skills_dirs()`/`restore_skills_dirs()`/`discard_skills_backup()` rename `~/.claude/skills` and `~/.agents/skills` aside before `apm install`, restore verbatim on failure, discard on success — same rename-never-delete discipline as `scripts/eval_arm.py`'s stash. `Sync.pinned_skill_sources()` reads `~/.apm/apm.lock.yaml` (stdlib line parser, no PyYAML) and `status`/`doctor` print `repo_url@short-sha` per pinned dependency. Failing-test-first: a test plants a `partial-write.txt` during a mocked failing `apm install` and asserts it is gone after `apply()` returns; the real-network isolated-HOME test above is the non-mocked confirmation |
| `[benched]` roster section + `validate_skill_bench`/`validate_skill_roster_delta` (agent-dotfiles#181) | self | **Author (2026-08-11)** | Eleven skills merged into `jonhill90/skills` on 2026-08-11 (`ask-a-council`, `distill`, `keep-me-honest`, `loop-contract`, `loop-memory`, `mine-transcripts`, `notify`, `prd`, `spec`, `tdd`, `verify-the-instrument`) were rostered nowhere and benched nowhere — an authored-but-unrostered skill was indistinguishable from a deliberately-withheld one, the same defect §10.1 rule 5 was written to prevent, now at the content layer instead of code (`dispatch.sh`'s six code instances). Per-skill intent was read from each skill's own `jonhill90/skills` issue rather than guessed from its name: five state rostering deferral explicitly (`loop-contract` jonhill90/skills#133 "Do not roster it on arrival", `loop-memory` jonhill90/skills#134, `verify-the-instrument` jonhill90/skills#135, `distill` jonhill90/skills#136, and the `keep-me-honest`/`prd`/`spec`/`tdd` batch's jonhill90/skills#129, "Adding any of these to the default roster is a separate step ... governed by the §10.1 evidence bar"); `mine-transcripts` (jonhill90/skills#137) and `notify` (jonhill90/skills#146, cross-checked against its shipped `SKILL.md` frontmatter) are designed user-invoked-only and were never proposed for the roster. `ask-a-council` (jonhill90/skills#147) is the weakest citation of the eleven: its issue never discusses rostering at all, so its bench reason is stated as general §10.1 policy rather than attributed to the issue (independent review on #185 flagged this; not grounded enough to withhold the skill, but not conflated with the nine issues that say so explicitly either). **None of the eleven cleared §10.1 rule 5** (no failed ×2 baseline, no passed ×3, no counter-scenario at ×2 exists for any of them), and the 2026-08-11 cost-gate exception a few rows above (PR #43, issue #45) named five specific, already-existing skills — it is not a standing policy, and none of the eleven was in it. All eleven are therefore benched, not rostered; rostering together would have cost ~1,367 tokens of description (measured against `jonhill90/skills` HEAD `b220b9e9`, real cost this time, unlike the ~367-token "no detectable cost" finding that justified the prior exception). `default-skills.txt` gained a `[benched]` section — a reserved name `scripts/sync.py` explicitly excludes from `roster_union()`/`neutral_union()` so it can never be folded into an install set the way a real `[harness]` section would be — with one `name  # reason` line per skill. `validate_skill_bench` fails a benched entry with an empty reason or a name that is both benched and rostered; `validate_skill_roster_delta` fails when a local `skills/` directory (test fixtures; #9 means none exists in the steady state) contains a name absent from both the roster union and the bench list. Independent review on #185 blocked the first version of this check: it skipped silently (`return []`) when `skills/` was absent, which is every real run of this repo since #9 — indistinguishable from "checked, found no orphans," and unlike `validate_roster_resolves`'s silent skip, there is no substitute instrument (`apm lock`) that would catch an unrostered-and-unbenched skill some other way. Fixed to emit a `warning` Finding naming the blind spot instead, matching the precedent set by the description-token cap's own no-`skills/` warning a few functions above. Mutation-checked both branches: stubbing the orphan-report line to return `[]` turns `test_orphaned_skill_is_reported` red (restored, green); stubbing the missing-`skills/` branch back to `return []` turns `test_missing_skills_dir_warns_instead_of_silent` red (restored, green). `apm.yml`'s public pin was also bumped `0ebaf91e` → `b220b9e9` (jonhill90/skills main HEAD) in the same change: the prior pin predated all eleven PRs, so even the "ship public opt-in" promise on their own issues was unreachable via `npx skills add` |
Prior migration decisions (vibes → skills, per-skill selection basis)
remain recorded in [migration-audit.md](migration-audit.md); they are not
re-litigated here.
