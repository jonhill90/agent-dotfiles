OpenAI Codex v0.147.0
--------
workdir: /private/tmp/council138
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: low
reasoning summaries: none
session id: 019ff21e-20e4-7681-a076-6a80aec58ff6
--------
user
You are answering specific placement questions about a real, existing set of repositories. Do not answer any abstract "is this a good layout" question — only the concrete questions below. Read the corpus first.

Four related git repositories, all owned by the same person, are being reviewed for documentation layout.

REPO agent-dotfiles — flat docs/, 12 files, all at top level under docs/:
- PRD.md (307 lines) — product requirements
- SPEC.md (1037 lines) — technical design spec
- handoff.md (378 lines) — living "current state" doc, rewritten not appended to
- harness-engineering.md (124 lines) — deployment model / harness boundaries
- loop-engineering.md (336 lines) — position doc on running unattended agent work
- memory.md (54 lines) — memory vault contract
- migration-audit.md (31 lines) — historical audit of a skill migration, table of decisions
- provenance-manifest.md (87 lines) — decision ledger (adopt/adapt/author/reject) citing sources
- supervisor-disposition.md (451 lines) — decide-ready comparison of two subsystems, "does not pick a winner"
- work-tracking.md (117 lines) — defines the issue tracker and what it is not
- agent-engineering-lineage.md (224 lines) — glossary/reference of external vocabulary with citations
- hierarchy-naming-57.md (393 lines) — research doc: external research, then applied to this repo, then a naming decision

REPO agent-evals — flat docs/, 1 file:
- evals.md (229 lines) — defines what the eval suite measures

REPO skills — NO docs/ directory. Top level: AGENTS.md, CLAUDE.md, README.md, scripts/, skills/, tests/. It is a public collection of skill content only.

REPO skills-private — NO docs/ directory. Top level: AGENTS.md, README.md, scripts/, skills/, tests/. Same shape as skills, private/authenticated.

A fifth, related repo (not in scope for this review, cited only as the origin of the pattern under question) uses:
docs/architecture/ (4 files), docs/decisions/ (26 files), docs/product/ (2 files, both carrying an explicit "Historical" banner), docs/runbooks/ (22 files), docs/reference/ (7 files).
Nobody deliberately designed that five-way split; it accreted while an AI coding agent built the repo, one file at a time, over several months.


Additional fact: on 2026-08-09, the `skills` repo's docs/ directory was deleted entirely, in a commit whose message says it was stripping "agent-dotfiles harness machinery" and other content not native to a standalone public skill collection, to make the repo a "standalone public skills collection." skills-private has never had a docs/ directory.
Additional fact: agent-dotfiles itself once had a docs/research/ subdirectory (10 files); on 2026-07-13 those files were deleted in a commit titled "Finish v1 PRD and acceptance," whose message says it will "distill temporary research into living documentation." The content was folded into flat top-level docs files (evals.md, harness-engineering.md, memory.md, etc.) and the subdirectory was removed, not kept alongside them.

ANSWER EACH, with a one-line reason for each:
1. Where should agent-dotfiles/docs/supervisor-disposition.md live — stay at docs/ top level, or move to a subdirectory? If a subdirectory, name it.
2. Where should agent-dotfiles/docs/migration-audit.md live, given it is explicitly a historical record and provenance-manifest.md is its stated successor?
3. Where should agent-dotfiles/docs/agent-engineering-lineage.md live, given it is explicitly "not a methodology" but a cited reference glossary?
4. Should agent-evals/docs/evals.md move anywhere, given it is the only file in that repo's docs/?
5. Should skills or skills-private gain a docs/ directory now, given the 2026-08-09 removal was deliberate? What would have to change for the answer to flip?
6. Given the 2026-07-13 removal of docs/research/ in agent-dotfiles, is reintroducing subdirectories under docs/ now consistent with that repo's own precedent, or does it reverse it?

Under 400 words total.

codex
1. `agent-dotfiles/docs/supervisor-disposition.md` should stay at `docs/` top level.  
Reason: it is an active decision-support document for current subsystem comparison, not a stable category that justifies a new directory by itself.

2. `agent-dotfiles/docs/migration-audit.md` should move to `docs/historical/` if any subdirectory is introduced.  
Reason: it is explicitly a historical audit, and `provenance-manifest.md` is the living successor.

3. `agent-dotfiles/docs/agent-engineering-lineage.md` should stay at `docs/` top level.  
Reason: it is an active cited reference glossary, not a methodology, runbook, or temporary research artifact.

4. `agent-evals/docs/evals.md` should not move.  
Reason: with one docs file, a subdirectory adds taxonomy without improving navigation.

5. `skills` and `skills-private` should not gain `docs/` now.  
Reason: `skills` deliberately removed docs to become a standalone public skills collection, and `skills-private` has never needed docs for the same repo shape; the answer flips only if either repo gains native, durable documentation about the skill collection itself that does not belong in `README.md`, `AGENTS.md`, individual skills, scripts, or tests.

6. Reintroducing subdirectories under `agent-dotfiles/docs/` now would mostly reverse that repo’s precedent, unless the new directory represents a durable document class rather than temporary research.  
Reason: the 2026-07-13 change removed `docs/research/` specifically to distill temporary research into living top-level docs, so recreating buckets for isolated or transitional files conflicts with that direction.
tokens used
16,922
1. `agent-dotfiles/docs/supervisor-disposition.md` should stay at `docs/` top level.  
Reason: it is an active decision-support document for current subsystem comparison, not a stable category that justifies a new directory by itself.

2. `agent-dotfiles/docs/migration-audit.md` should move to `docs/historical/` if any subdirectory is introduced.  
Reason: it is explicitly a historical audit, and `provenance-manifest.md` is the living successor.

3. `agent-dotfiles/docs/agent-engineering-lineage.md` should stay at `docs/` top level.  
Reason: it is an active cited reference glossary, not a methodology, runbook, or temporary research artifact.

4. `agent-evals/docs/evals.md` should not move.  
Reason: with one docs file, a subdirectory adds taxonomy without improving navigation.

5. `skills` and `skills-private` should not gain `docs/` now.  
Reason: `skills` deliberately removed docs to become a standalone public skills collection, and `skills-private` has never needed docs for the same repo shape; the answer flips only if either repo gains native, durable documentation about the skill collection itself that does not belong in `README.md`, `AGENTS.md`, individual skills, scripts, or tests.

6. Reintroducing subdirectories under `agent-dotfiles/docs/` now would mostly reverse that repo’s precedent, unless the new directory represents a durable document class rather than temporary research.  
Reason: the 2026-07-13 change removed `docs/research/` specifically to distill temporary research into living top-level docs, so recreating buckets for isolated or transitional files conflicts with that direction.
