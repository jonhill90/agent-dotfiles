PASS: final message contains actual command output/results, not just assertions.

Mode note (2026-07-12): in interactive mode the transcript itself
carries tool output (visible test runs); the "paste output in final
message" criterion is print-mode-biased. Interactive: PASS if the
transcript shows the verification commands ran. Pi passes even in
print mode (overlay gate).
