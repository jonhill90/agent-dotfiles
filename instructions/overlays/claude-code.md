# Claude Code Overlay

Intentionally empty. Claude Code is the thickest harness: plan mode,
permission gates, verification norms, and auto-memory are native, so
the canonical instructions need no reinforcement here (SPEC §3.2
thinning; harness-baselines Finding 3 — truth-pass 2026-08-23: could not
measure this citation against anything in the current tree, no doc named
`harness-baselines` exists here; plausibly a private agent-evals finding
cited by name per this repo's own convention, but unverifiable from this
repository alone).

Add content only when an eval run shows a gap on Claude Code that the
canonical instructions do not cover — and record the deciding results
file in the provenance manifest before doing so (a thinning check under
E3/E9 with the behavioral layer off — cited here as "SPEC §10", but
truth-pass 2026-09-11 found current SPEC.md jumps §3.6 → §14.1 directly;
§10's content is gone, not renumbered, so there is no live section to
repoint this to. The same gap reaches README.md's §4/§10.1 citations and
others across this repo — not fixed here, flagged only, per
agent-estate#1397).
