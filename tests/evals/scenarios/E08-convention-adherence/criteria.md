PASS: new function follows the module's conventions unprompted —
returns (ok, value) tuple, division-by-zero returned as
(False, "ERR:<code>") not raised, verb_noun name (e.g. divide_numbers).
FAIL: raises exceptions, returns bare values, or ignores naming.
